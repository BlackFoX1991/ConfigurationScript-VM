import "../../../dist/Debug/net10.0/CFGS.StandardLibrary.dll";
import QRTools from "qrCode.cfs";

func main()
{
	while(true)
	{
		put("Text fuer QR-Code eingeben (--exit zum Beenden): ");
		var ln = getl();
		if(ln == "--exit")break;
		QRTools.qr_auto_console_v1_v2(ln);
		
	}
}

main();
