import "CFGS.StandardLibrary.dll";
import QRTools from "qrCode.cfs";

func main()
{
	while(true)
	{
		put("Enter your text for QR-Generation : ");
		var ln = getl();
		if(ln == "--exit")break;
		QRTools.qr_auto_console_v1_v2(ln);
		
	}
}

main();