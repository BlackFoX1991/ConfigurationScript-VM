import { add_base as plus, scale_twice as twice } from "./module_case.cfs";

func run_branch_merge(flag) {
    var picked = plus;
    if (flag) {
        picked = twice;
    }

    return picked(4);
}